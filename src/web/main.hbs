<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenKDS - {{station.pretty_name}}</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background-color: #000;
            color: #000;
            font-size: 2em;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
        }
        .orderContainer {
            display: grid;
            grid-template-rows: repeat(2, 1fr);
            grid-template-columns: repeat(2, 1fr);
            height: 80vh;
            width: 85vw;
            grid-gap: 10px;
            margin: 0 auto;
        }

        .order {
            background-color: #ccc; /* set a background color for the divs */
            border-radius: 10px;
            padding: 10px;
            color: #000;
        }
        .footer{
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 10vh;
            color: #fff;
            border-radius: 10px;
        }

        .stationInfo{
            position: absolute;
            bottom: 10px;
            color: white;
            left: 50%;
            transform: translateX(-50%);

        }
        .pendingInfo{
            position: absolute;
            bottom:0;
            right:0;
            background-color: #fff;
            color: #000;
            padding: 10px;
        }
    </style>

</head>
<body>

    This is {{ station.pretty_name }}!

    <div class='main'>
        
    </div>

    <div class="orderContainer">
        {{#each orders}}
            <div class="order" order-id="{{this.id}}">
                <p>{{this.id}}</p>
                {{#each this.items}}
                    <p>{{this.name}}</p>
                {{/each}}
                <p>{{this.time_since_order}}</p>
            </div>
        {{/each}}
    </div>

    
    <div class='footer'>
        <p class="stationInfo">{{station.pretty_name}} - <span style="color: green">ON</span> - 132/258</p>
        
        {{#if pending}}
            <div class="pendingInfo">
                <p>{{pending}} orders pending >></p>
            </div>
        {{/if}}
    </div>




</body>
<script>
    let orderDivs = document.querySelectorAll('.order');
    if(orderDivs.length > 4){
        for(let i = 4; i < orderDivs.length; i++){
            orderDivs[i].style.display = 'none';
        }
    }
</script>
</html>